name: Update ReaPack index

on:
  push:
    branches: [main]

permissions:
  contents: write

jobs:
  reapack-index:
    runs-on: ubuntu-latest
    steps:
      - name: Fetch repository history
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - name: Configure Git
        run: |
          git config user.name "ReaPack Index Bot"
          git config user.email "306310608+janschwartzmusic@users.noreply.github.com"
      - name: Install Pandoc
        run: sudo apt-get update && sudo apt-get install -yy pandoc
      - name: Set up Ruby
        uses: ruby/setup-ruby@v1
        with:
          ruby-version: "3.2"
      - name: Install reapack-index
        run: gem install reapack-index
      - name: Update index.xml
        run: reapack-index --commit
      - name: Push index commit
        run: git push
